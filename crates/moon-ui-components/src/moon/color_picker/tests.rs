// Populated by `breakage-prover` at the closing fix barrier (task-gates rule: unit tests are
// never authored by hand in this task). Module scaffolding only — declared so `mod tests;` in
// `color_picker.rs` resolves for `cargo fmt`/`cargo check --all-targets`/`cargo test`.
